spring:
  datasource:
    url: ${TEST_DATABASE_URL:jdbc:h2:mem:resolve-log-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1}
    username: ${TEST_DATABASE_USER:sa}
    password: ${TEST_DATABASE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
app:
  jwt-secret: resolve-log-integration-test-secret-key-only-for-tests-2026
  timezone: Asia/Seoul

